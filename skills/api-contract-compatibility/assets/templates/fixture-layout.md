# Fixture Layout

```text
contracts/
  baseline/
    create_user.response.json
    user_created.event.json
  candidate/
    create_user.response.json
    user_created.event.json
```

Use identical relative paths in baseline and candidate directories.
